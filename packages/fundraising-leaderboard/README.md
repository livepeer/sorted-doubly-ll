# Testing

```
NODE_ENV=test zos push --deploy-libs --network local
```

```
NODE_ENV=test zos publish --network local
```

```
NODE_ENV=test zos create FundraisingLeaderboard --init initialize --args <APP_ADDRESS> <BENEFICIAR_ADDRESS> <MAX_CONTRIBUTORS> --network local
```